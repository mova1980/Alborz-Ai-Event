# سیستم طراحی UI/UX — خانواده سایت‌های هفته تکنولوژی البرز (ATW)

**نام سیستم:** ATW Dark Geometry  
**نسخه:** ۱.۰ · مهر ۱۴۰۵ / October 2026  
**مرجع زنده:** سایت هفته تکنولوژی البرز ۱۴۰۵  
**هدف این فایل:** هر برنامه‌نویس، بدون دیدن کد منبع، بتواند سایت یا پنل زیرمجموعه را با **همین وحدت رویه** بسازد یا پوسته‌اش را عوض کند.

این سند تم **رویداد البرز** است، نه تم نشریات و نه تم وکالت. گوشه گرد، طلایی، بنفش، و دکمه‌های کپسولی ممنوع است.

---

## ۰. قوانین غیرقابل مذاکره

1. **تم همیشه تیره است.** پس‌زمینه صفحه `#020810`. تم روشن برای کل سایت ساخته نشود.
2. **تنها رنگ برند آبی `#0055FF` است.** هیچ رنگ اکسنت دیگری (طلا، فیروزه، بنفش، گرادیان رنگین‌کمانی) روی دکور، لوگو، دکمه، کرسر، یا خط استفاده نشود.
3. **هندسه تیز.** `border-radius: 0` روی کارت، دکمه، اینپوت، مودال، چیپ، سایدبار. دایره فقط برای کره زمین و ستاره مجاز است.
4. **متوازی‌الاضلاع و لوزی زبان شکل‌اند** — لوگو، کرسر، آیکون شبکه اجتماعی، کلیپ تصویر، نشان فعال ناوبری.
5. **فارسی پیش‌فرض، انگلیسی کامل.** `lang="fa"` و `dir="rtl"` پیش‌فرض. انگلیسی مسیر `/en` یا معادل. ترجمه سطحی ممنوع.
6. **ارقام فارسی در FA، لاتین در EN.** همه اعداد نمایشی. مسیر URL و ایمیل مستثنا.
7. **حروف فارسی را هرگز به `span` تک‌حرفی نشکن.** اتصال حروف از بین می‌رود.
8. **هاور فقط با ماوس.** `@media (hover: hover) and (pointer: fine)`.
9. **اسکرول افقی ممنوع.** از `100vw` / `160vw` برای عرض استفاده نکن.
10. **`prefers-reduced-motion: reduce` همه انیمیشن را قطع کند.**

اگر یکی از این ده مورد نقض شود، سایت عضو خانواده ATW نیست.

---

## ۱. هویت بصری

### ۱.۱ pallete — کپی کن

```css
:root {
  --navy-950: #020810;   /* صفحه، پیش‌لودر، اورلی */
  --navy-900: #081421;   /* هدر اسکرول‌شده، سایدبار ادمین، نقل‌قول */
  --navy-800: #0f2842;   /* کارت تیره، ردیف جدول */
  --navy-700: #163c63;   /* خط / بوردر — همان --line */
  --navy-600: #1d5084;   /* هاور ردیف، چارت */
  --navy-100: #d4e0ed;
  --navy-50:  #f2f5f9;   /* متن اصلی روی تیره = --ink */

  --blue-500: #0055FF;   /* برند — تنها رنگ اکشن */
  --blue-400: #1a75ff;   /* فوکوس */
  --blue-300: #4d94ff;   /* کیکر، ساعت برنامه */
  --blue-200: #80b3ff;   /* لیبل، تاریخ */
  --blue-100: #b3d1ff;   /* زیرعنوان هیرو */

  --ink:    #f2f5f9;
  --muted:  rgba(242, 245, 249, 0.62);
  --line:   #163c63;
  --brand:  var(--blue-500);

  --ease: cubic-bezier(0.22, 1, 0.36, 1);
  --header-h: 82px;
}
```

لایه‌های سطح صفحه (عمومی):

| سطح | رنگ | کاربرد |
|-----|------|--------|
| 0 | `#020810` | بادی، هیرو |
| 1 | `#07101c` | نوارهای میانی (سرخط، زون) |
| 2 | `#081421` | بلوک نقل‌قول، آمار، پارتنر |
| 3 | `#050b14` | فوتر |
| خط | `#163c63` | همه بوردرها |
| اکشن | `#0055FF` | دکمه، لینک فعال، عدد آمار، کرسر |

**گرادیان مجاز:** فقط آبی روی سورمه، برای هاله زمین یا پر شدن کارت.

```css
/* هاله زمین */
radial-gradient(circle, rgba(0,85,255,0.22) 0%, rgba(0,85,255,0.05) 48%, transparent 70%)

/* پر شدن کارت روی هاور */
linear-gradient(165deg, rgba(0,85,255,0.22), transparent 58%)
```

**ممنوع:** گرادیان طلا، بنفش، سبز تزئینی، `text-shimmer` رنگین‌کمانی روی عنوان.

رنگ معنایی **فقط داخل پنل ادمین و فقط برای داده** (نه دکور):

| معنا | رنگ | نمونه |
|------|------|--------|
| موفق | `#34d399` با opacity کم | وضعیت تایید |
| خطا | `#f87171` با opacity کم | رد / حذف |
| هشدار | `#fbbf24` با opacity کم | در انتظار |

روی سایت عمومی این سه رنگ دیده نشوند مگر در پیام خطای فرم.

### ۱.۲ تایپوگرافی

| نقش | فارسی | انگلیسی |
|-----|--------|----------|
| متن و UI | **Modam** → **IRANSans** → **Vazirmatn** | **Outfit** |
| نمایش / عنوان | همان stack فارسی | **Syne** سپس Outfit |

```css
--font-fa: 'Modam', 'IRANSans', 'Vazirmatn', sans-serif;
--font-en: 'Outfit', 'Syne', sans-serif;
--font-display-en: 'Syne', 'Outfit', sans-serif;

html[lang='fa'] { font-family: var(--font-fa); }
html[lang='en'] { font-family: var(--font-en); }
html[lang='en'] .display,
html[lang='en'] h1,
html[lang='en'] h2 { font-family: var(--font-display-en); letter-spacing: -0.03em; }
```

وزن‌ها: ۳۰۰ نقل‌قول، ۴۰۰ متن، ۵۰۰ عنوان بخش، ۶۰۰ عنوان هیرو / دکمه، ۷۰۰ نام برند.

مقیاس:

| عنصر | اندازه |
|------|--------|
| کیکر / لیبل | 11–12px، letter-spacing 0.18–0.22em، uppercase در EN |
| متن بدنه | 14–17px، line-height 1.7 |
| عنوان بخش | `clamp(28px, 4.4vw, 52px)` وزن ۵۰۰ |
| عنوان صفحه داخلی | `clamp(40px, 7vw, 84px)` وزن ۵۰۰ |
| عنوان هیرو | `clamp(32px, 6.4vw, 86px)` وزن ۶۰۰ + `text-shadow: 0 0 24px rgba(0,85,255,0.35)` |
| زیرعنوان هیرو | `clamp(18px, 3.2vw, 40px)` رنگ `--blue-100` |
| عدد آمار | `clamp(36px, 5vw, 72px)` رنگ `--brand` |
| دکمه CTA | 12px، uppercase، letter-spacing 0.14em، وزن ۶۰۰ |

روی موبایل عنوان هیرو: `clamp(26px, 8.6vw, 40px)`.

فونت محلی: فایل‌ها در `/public/fonts/Modam/` و `/public/fonts/IRANSans/`. اگر فایل نبود، Vazirmatn از CDN/`@fontsource` کافی است. آپلود فونت را اجباری نکن؛ fallback باید سالم باشد.

### ۱.۳ ارقام

```js
const FA = '۰۱۲۳۴۵۶۷۸۹'
function localizeDigits(value, lang) {
  const s = String(value)
  if (lang === 'fa') {
    return s.replace(/[0-9]/g, d => FA[d])
            .replace(/[٠-٩]/g, d => FA[d.charCodeAt(0) - 0x0660])
  }
  return s.replace(/[۰-۹]/g, d => String(d.charCodeAt(0) - 0x06F0))
          .replace(/[٠-٩]/g, d => String(d.charCodeAt(0) - 0x0660))
}
```

اعمال روی: شمارش معکوس، آمار، تاریخ، ساعت، شماره کارت، تلفن نمایشی، ایندکس جدول.  
اعمال نشود روی: `href`، ایمیل، کد CSS، ورودی زنده‌ای که کاربر می‌نویسد (مگر نمایش جدا).

`font-variant-numeric: tabular-nums` روی اعداد شمارشی.

---

## ۲. هندسه — زبان شکل

همه چیز از **لوزی** و **متوازی‌الاضلاع** مشتق می‌شود. گوشه شعاع‌دار زبان این سیستم نیست.

### ۲.۱ لوزی (علامت برند)

```svg
<svg viewBox="0 0 42 28">
  <polygon points="8,26 24,2 34,2 18,26" />
</svg>
```

- لوگو: پر `#0055FF` + `drop-shadow: 0 0 10px rgba(0,85,255,0.55)`
- کرسر هسته: لوزی ۷×۷ همان رنگ
- شبکه اجتماعی فوتر: متوازی‌الاضلاع پر؛ هاور = فقط استروک

### ۲.۲ متوازی‌الاضلاع (رسانه و هیرو)

```css
/* تصویر کشیده — LTR */
clip-path: polygon(28% 0, 100% 0, 72% 100%, 0 100%);

/* تصویر کشیده — RTL */
clip-path: polygon(0 0, 72% 0, 100% 100%, 28% 100%);

/* قاب رسانه داخلی صفحه */
clip-path: polygon(8% 0, 100% 0, 92% 100%, 0 100%);
```

روی موبایل درصد را کم کن (حدود ۱۴٪ / ۸۶٪) و عرض را `100%` بگذار — هرگز `160vw`.

### ۲.۳ کرسر سفارشی

فقط `pointer: fine` و در صورت نبودن `prefers-reduced-motion`.

- هسته: لوزی ۷px، دنبال ماوس بدون تأخیر
- حلقه: SVG متوازی‌الاضلاع ۴۲×۲۸، lerp `0.18`
- هاور روی لینک/دکمه: حلقه scale 1.35 و fill `rgba(0,85,255,0.14)`
- کلیک: scale 0.72
- `cursor: none` روی صفحه؛ روی input/textarea/select کرسر متن برگردد

### ۲.۴ نویز فیلم

لایه ثابت تمام‌صفحه، `z-index: 80`، `opacity: 0.035`، `mix-blend-mode: overlay`، SVG `feTurbulence`. تزئینی است؛ روی ادمین هم مجاز، روی فرم ادمین اختیاری.

---

## ۳. موشن

```css
--ease: cubic-bezier(0.22, 1, 0.36, 1);
```

| الگو | مشخصات | کاربرد |
|------|---------|--------|
| ورود بخش | `opacity 0 → 1` + `translateY(24px)`، 600ms، stagger 80–120ms | کارت، استپ، سرخط |
| پر شدن ناوبری | `background-size: 0 100% → 100% 100%` از پایین، 350ms | لینک هدر دسکتاپ |
| چهارخط CTA | چهار ضلع دکمه از بیرون به داخل، 500ms | دکمه اصلی |
| کلیپ زمین | `clip-path` 750ms با تأخیر 200ms | هیرو |
| اسلاید زیرعنوان | `top` 800ms با تأخیر ~5s | جفت زیرعنوان هیرو |
| درخشش کارت | نوار سفید مورب `translateX` 700ms | هاور کارت |
| پیش‌لودر | stroke-dashoffset 1100ms سپس fill | لوگوی لوزی |

ورود هیرو: کپی متن از سمت داخل (`translateX ±40px`) بعد از 800ms.

عنوان هیرو را **کلمه کامل** انیمیت کن (نه حرف):  
- جغرافی / مکان: بالا آمدن + هاله آبی + درخشش overlay  
- فناوری: rotateX + خط اسکن آبی  
- اتصال: ورود از کنار + خط متوازی‌الاضلاع زیر کلمه  

زمین: چرخش CSS 120s یا WebGL آهسته؛ روی موبایل / `pointer: coarse` فقط PNG + چرخش CSS. `mix-blend-mode: screen` + ماسک شعاعی.

Lenis / smooth scroll فقط دسکتاپ fine-pointer. موبایل اسکرول بومی.

---

## ۴. پوسته سایت عمومی

### ۴.۱ هدر

```
[ لوزی + نام سایت ]  |  [ نشان میزبان ]     ناوبری وسط     [ FA|EN ] [ جستجو ] [☰]
```

- `position: sticky; top: 0`
- بوردر پایین `--line`
- اسکرول: پس‌زمینه `rgba(8,20,33,0.92)` + `backdrop-filter: blur(16px)` + ارتفاع فشرده ~۵۶–۵۸px
- ناوبری دسکتاپ (≥۱۱۰۰px): لینک uppercase، پر شدن آبی از پایین روی هاور و صفحه جاری
- منوی موبایل: کشو از داخل صفحه (RTL از راست)، عرض `min(88vw, 360px)`، پس‌زمینه `--navy-900`، backdrop تیره، بدون هل دادن کل صفحه
- همبرگر: سه خط با spring به ضربدر آبی
- سوییچ زبان: دو خانه چسبیده، بوردر `--line`، فعال = پس‌زمینه `#0055FF` سفید. شکل سوییچ را عوض نکن
- زیر ۷۲۰px نشان میزبان را پنهان کن؛ زیر ۴۸۰px زیرعنوان لوگو را پنهان کن

`z-index`: هدر 2000، منو 1999، backdrop 1998.

### ۴.۲ دکمه اصلی — چهار خط (اجباری)

دکمه برند این سیستم **کپسول گرد نیست**. چهار ضلع جداگانه از بیرون به داخل می‌آیند؛ یک خط افقی کوتاه سمت شروع متن.

پدینگ LTR: `16px 22px 16px 52px` — RTL برعکس.  
هاور: چهار خط کامل + پس‌زمینه `rgba(0,85,255,0.12)`.

روی موبایل در ردیف CTA: تمام‌عرض، مساوی، خط کناری متن را می‌توان پنهان کرد.

سه CTA هم‌وزن اگر وجود داشته باشند — هیچ‌کدام ثانویه بصری نیست.

### ۴.۳ هیرو

لایه‌ها از پشت به جلو:

0. `#020810`  
1. ستاره‌ها (~۴۸ نقطه سفید، twinkle 2–6s) — دایره مجاز  
2. زمین داخل کلیپ متوازی‌الاضلاع (~۵۸٪ عرض دسکتاپ)  
3. هاله آبی blur  
4. خط‌هنری SVG استروک برند (اختیاری، دسکتاپ)  
5. کپی: کیکر → عنوان → زیرعنوان اسلایدشونده → لید → شمارش (اگر رویداد) → CTA  
6. اندیکاتور اسکرول: خط عمودی گرادیان برند؛ در موبایل پنهان

دسکتاپ: ردیف معکوس، زمین بزرگ، متن پایین.  
موبایل: ستون، زمین حداکثر ~۴۲svh، بدون overflow افقی.

### ۴.۴ بخش‌های تکرارشونده

**کارت سرخط / خبر**
- بوردر `--line`، پس‌زمینه `rgba(8,20,33,0.62)`
- گوشهٔ L شکل برند در دو قطر
- ایندکس دو رقمی آبی
- هاور: `translateY(-6px)` + پر شدن گرادیان از پایین + درخشش مورب + پیکان متوازی‌الاضلاع
- ورود: stagger از پایین — نه `left: -50px` (overflow می‌سازد)

**زون / قابلیت**
- بوردر ۱px، حداقل ارتفاع ~۱۴۸px
- هاور: بوردر برند + لایه `rgba(0,85,255,0.12)` که عرضش ۰→۱۰۰٪ می‌شود

**استپ / فرایند**
- فقط `border-top: 2px solid #0055FF`
- شماره `۰۱` آبی، عنوان، توضیح muted

**آمار**
- عدد بزرگ برند، لیبل کوچک muted
- گرید ۲ سپس ۴ ستون

**نقل‌قول**
- متن وزن ۳۰۰، خط افقی ۱۳۰×۱px برند بالای منبع

**تصویر + متن (pull)**
- تصویر کلیپ متوازی‌الاضلاع، متن `clamp(22px, 2.4vw, 34px)` وزن ۳۰۰

** marquee / ghost text**
- یک خط بزرگ opacity 0.22، حلقه ۲۸s. در RTL جهت برعکس

**چیپ**
- بوردر `--line`، پدینگ `8px 12px`، شعاع صفر

**کارت صفحه داخلی**
```css
.card {
  border: 1px solid var(--line);
  padding: 22px;
  background: rgba(15, 40, 66, 0.35);
}
```

### ۴.۵ فرم عمومی

- لیبل: 12px، uppercase، `--blue-200`
- فیلد: پس‌زمینه `rgba(8,20,33,0.6)`، بوردر `--line`، پدینگ `12px 14px`، **radius 0**
- فوکوس: بوردر `--brand`، بدون outline پیش‌فرض؛ `:focus-visible` روی کنترل‌ها `2px solid #1a75ff`
- موفقیت: فقط بوردر برند + متن `--blue-100`
- اینپوت موبایل حداقل 16px (جلوگیری از زوم iOS)

### ۴.۶ جستجو

اورلی تمام‌صفحه `rgba(2,8,16,0.92)`، اینپوت بزرگ بدون باکس، فقط خط سفید زیر، z-index 9999.

### ۴.۷ فوتر

`#050b14`. ردیف بالا: آیکون متوازی‌الاضلاع شبکه / تلفن / مکان. پایین: قفل لوگو + کپی + ایمیل. لینک هاور: underline offset 5px به رنگ برند.

### ۴.۸ پیش‌لودر

تمام‌صفحه `#020810`، رسم استروک لوزی سپس پر شدن برند، متن uppercase پایین. حدود ۱.۴ ثانیه. z-index 9000.

### ۴.۹ صفحات داخلی

- خرده‌نان uppercase 11px
- عنوان خیلی بزرگ
- خط‌هنری SVG کم‌رنگ در گوشهٔ خارج
- گرید کارت ۲ / ۳ ستونه از ۷۰۰ / ۹۰۰px

---

## ۵. دوزبانه و RTL

```js
document.documentElement.lang = isFa ? 'fa' : 'en'
document.documentElement.dir  = isFa ? 'rtl' : 'ltr'
```

- پیش‌فرض فارسی، انگلیسی زیر `/en/...`
- از `margin-inline` / `inset-inline` / `border-inline-start` استفاده کن نه left/right
- منوی موبایل در RTL از راست باز شود
- سوییچ FA | EN شکل ثابت؛ فقط خانه فعال عوض شود
- محتوای EN بازنویسی واقعی است نه Google Translate
- در فارسی letter-spacing زیاد روی متن بدنه نگذار؛ tracking فقط برای کیکر و EN display

اسپای SPA روی Vercel:

```json
{ "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }] }
```

بدون این، `/en` و صفحات داخلی ۴۰۴ می‌شوند.

---

## ۶. ریسپانسیو

بریک‌پوینت عملی این سیستم:

| عرض | رفتار |
|-----|--------|
| < 480 | قفل برند خیلی فشرده، CTA ستونی |
| < 640 | هیرو تک‌ستون، عنوان کوچک‌تر، زیرعنوان دخط |
| < 700 | کارت خبر ۱ ستون، فوتر ستونی |
| < 1100 | منوی همبرگر، استپ و زون ۲ ستون |
| ≥ 1100 | هدر سه‌ستونه، ناوبری افقی |
| ≥ 1200 | سرخط ۴ ستون، استپ ۵ ستون |

لمس: هدف حداقل ۴۴px.  
`viewport-fit=cover` + `env(safe-area-inset-*)`.  
`overflow-x: clip` روی `html, body`.  
هاور چسبنده لمسی را با media hover خنثی کن.

---

## ۷. پنل مدیریت — مدل اجباری خانواده ATW

سایت عمومی ادمین ندارد؛ **هر زیرسایت که پنل می‌سازد باید از این مدل استفاده کند** تا وحدت رویه حفظ شود. ادمین ادامه همان زبان است، نه داشبورد Bootstrap و نه کارت گرد Tailwind.

### ۷.۱ اصول ادمین

- همان توکن‌های رنگ و فونت و شعاع صفر
- چگالی بالاتر از سایت عمومی؛ بدون زمین چرخان، بدون ستاره، بدون marquee
- نویز فیلم اختیاری با opacity نصف
- کرسر سفارشی فقط دسکتاپ؛ در جدول و اینپوت کرسر سیستم
- FA پیش‌فرض، EN کامل، ارقام مطابق زبان
- سایدبار ثابت دسکتاپ؛ در موبایل کشو همان الگوی منوی سایت

### ۷.۲ ساختار

```
┌─────────────────────────────────────────────────────────────────┐
│  لوزی  نام‌پنل          جستجوی فرمان         FA|EN   آواتار لوزی │
├──────────────┬──────────────────────────────────────────────────┤
│ داشبورد      │  کیکر بخش                                        │
│ محتوا        │  عنوان صفحه           [ CTA چهارخطی افزودن ]     │
│ رسانه        │──────────────────────────────────────────────────│
│ درخواست‌ها   │  [KPI] [KPI] [KPI] [KPI]                         │
│ کاربران      │──────────────────────────────────────────────────│
│ تنظیمات      │  فیلترها / جدول یا فرم                           │
│———————       │                                                  │
│ خروج         │  پجینیشن خطی                                     │
└──────────────┴──────────────────────────────────────────────────┘
```

عرض سایدبار: 240px دسکتاپ. پس‌زمینه `--navy-900`، بوردر inline `--line`.

آیتم سایدبار:
- پدینگ `12px 16px`، uppercase 12px، tracking 0.12em
- فعال: پر شدن `#0055FF` از لبه شروع (همان الگوی ناوبری سایت) + متن سفید
- آیکون: استروک ۲px، هندسی، نه آیکون ابری رنگارنگ
- نشان تعداد: مربع تیز برند، رقم فارسی/لاتین

هدر ادمین: sticky، ارتفاع ۵۶px، blur مثل هدر سایت.

### ۷.۳ نقشه اطلاعات (برای هر زیرسایت تطبیق بده، شکل را عوض نکن)

| ماژول | کار |
|--------|-----|
| داشبورد | ۴ KPI، نمودار خطی تک‌رنگ برند، فعالیت اخیر |
| محتوا | درخت/جدول صفحات، خبر، برنامه، زون — وضعیت پیش‌نویس/منتشر |
| رسانه | گرید تیز تصاویر، کلیپ متوازی‌الاضلاع روی بندانگشتی هاور |
| درخواست‌ها | بازدید، غرفه، مسئله، تماس — تایید / رد |
| کاربران | نقش: Super / Editor / Reviewer / Viewer |
| تنظیمات | زبان‌ها، قفل رنگ (غیرقابل تغییر از UI)، دامنه، اسکریپت‌ها |
| رکورد | لاگ ورود و انتشار |

نقش‌ها حداقل همین چهار تا باشند. رنگ نقش نساز؛ نقش با متن muted مشخص شود.

### ۷.۴ KPI

```
┌────────────┐
│ ۰۴          │  عدد بزرگ برند
│ درخواست نو  │  لیبل muted
│ ▂▄▆█ خط spark │  فقط #0055FF
└────────────┘
```

بوردر `--line`، پدینگ ۲۲px، شعاع صفر. هاور مثل کارت زون (پر شدن آبی ۱۲٪).

### ۷.۵ جدول

```css
.admin-table { width: 100%; border-collapse: collapse; }
.admin-table th {
  text-align: start;
  font-size: 11px;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: var(--blue-200);
  border-bottom: 1px solid var(--line);
  padding: 12px 14px;
  background: rgba(15, 40, 66, 0.5);
}
.admin-table td {
  padding: 14px;
  border-bottom: 1px solid var(--line);
  font-size: 14px;
}
.admin-table tr:hover td { background: rgba(0, 85, 255, 0.08); }
```

اکشن ردیف: لینک‌های متنی uppercase 11px. ویرایش رنگ برند. حذف فقط در confirm مودال — روی خود جدول قرمز تزئینی نگذار؛ در مودال تایید می‌توان متن خطا را `--red` کم‌فروغ کرد.

وضعیت‌ها با **چیپ تیز** نه نقطه رنگی بزرگ:

- منتشر / تایید: متن برند، بوردر برند
- پیش‌نویس: muted
- در انتظار: بوردر `--line`
- رد شده: متن muted، خط روی متن

### ۷.۶ فرم ادمین

همان فرم عمومی. عرض حداکثر 720px در ستون محتوا. لیبل بالای فیلد. گروه‌بندی با `border-top: 2px solid #0055FF` مثل استپ‌های سایت.

سوییچ تنظیمات: مربع تیز، فعال = پر برند. توگل کپسولی ممنوع.

آپلود فایل: ناحیه درگ با بوردر خط‌چین `--line` (خط‌چین تنها استثنای «نرم» است)، هاور بوردر برند. پیش‌نمایش تصویر با `clip-path` متوازی‌الاضلاع خفیف.

### ۷.۷ مودال ادمین

- اسکریم `rgba(2,8,16,0.8)` + blur 8px
- پنل: `--navy-900`، بوردر `--line`، شعاع صفر، حداکثر 560px
- ورود: `opacity + translateY(16px)` 400ms `--ease`
- عنوان وزن ۵۰۰، CTA چهارخطی برای تایید، متنی uppercase برای لغو
- z-index 9100 (بالای پیش‌لودر سایت اگر مشترک باشد: 9200)

### ۷.۸ ورود به پنل

صفحه جدا، بدون سایدبار.

- پس‌زمینه `#020810`
- لوزی بزرگ استروک (همان پیش‌لودر) بالا
- نام سیستم + «ورود مدیران»
- دو فیلد: شناسه، گذرواژه
- CTA چهارخطی «ورود»
- سوییچ FA|EN گوشه
- بدون Google button رنگی، بدون کارت سفید، بدون rounded

پس از ورود ناموفق: بوردر فیلد برند نمی‌شود؛ پیام زیر فرم رنگ `--blue-100` یا متن خطا کم‌فروغ. رمز را در سند ننویس.

جلسه: httpOnly cookie یا معادل؛ نقش از سرور. UI نقش را فقط برای مخفی کردن آیتم سایدبار بخواند.

### ۷.۹ نمودار

یک رنگ: `#0055FF`. گرید خط `--navy-700`. بدون گرادیان سه‌رنگ، بدون نمودار دونات رنگارنگ. اسپارک‌لاین در KPI کافی است؛ صفحه گزارش یک line + یک bar تک‌رنگ.

### ۷.۱۰ ادمین موبایل

سایدبار کشو. KPI دو ستون. جدول به کارت‌های تیز تبدیل شود (هر ردیف یک کارت با بوردر `--line`). CTA افزودن چسبیده پایین با safe-area.

---

## ۸. احراز هویت (مدل)

برای وحدت رویه، نه برای کپی رمز:

```
roles: super | editor | reviewer | viewer
login(identifier, password) -> session
logout()
can(role, action)
```

صفحه عمومی فرم‌های بازدید/غرفه/مسئله را به صف «درخواست‌ها» می‌فرستد. ادمین همان صف را با جدول ۷.۵ مدیریت می‌کند.

---

## ۹. لایه z-index

| لایه | z |
|------|---|
| خط‌هنری پس‌زمینه صفحه | -1 |
| محتوا | 1–10 |
| نویز | 80 |
| هدر | 2000 |
| منو / سایدبار موبایل | 1999 |
| backdrop منو | 1998 |
| پیش‌لودر | 9000 |
| جستجو | 9999 |
| مودال ادمین | 9200 |
| کرسر | 10050 |
| skip link فوکوس | 10000 |

---

## ۱۰. دسترسی و کیفیت

- `:focus-visible { outline: 2px solid #1a75ff; outline-offset: 3px; }`
- لینک پرش به محتوا
- `aria-expanded` روی همبرگر، `aria-live` روی اسلاید هیرو
- کنتراست متن `--ink` روی `#020810` و muted حداقل برای لیبل‌ها با `--blue-200`
- تصاویر `alt`؛ تصاویر دکور خالی
- کاهش حرکت = بدون انیمیشن و بدون کرسر سفارشی

---

## ۱۱. آنچه هرگز نباید وارد زیرسایت شود

- `border-radius` بزرگ، `rounded-full`، دکمه کپسولی
- رنگ طلا / بنفش / گرادیان رنگین‌کمانی عنوان
- قفسه کتاب، اسپاین سه‌بعدی مجله
- فونت Plus Jakarta به‌عنوان هویت (جایگزین: Outfit / Syne)
- زبان آلمانی به‌عنوان زبان سیستم (مگر محتوای همان سایت)
- سایه نرم بزرگ iOS (`shadow-2xl` رنگی غیر آبی)
- کارت شیشه‌ای blur به‌جای بوردر تیز — blur فقط هدر/اورلی
- شکستن حروف فارسی
- ارقام لاتین در UI فارسی
- اسکرول افقی ناشی از clip 160vw
- آیکون شبکه داخل دایره رنگی پلتفرم (به‌جایش متوازی‌الاضلاع برند)

---

## ۱۲. پوسته‌کردن سایت موجود (چک‌لیست برنامه‌نویس)

1. CSS variables بخش ۱.۱ را در `:root` بگذار.  
2. همه `border-radius` را ۰ کن.  
3. رنگ اکشن را به `#0055FF` قفل کن.  
4. فونت‌ها را طبق ۱.۲ عوض کن.  
5. هدر را sticky تیره + سوییچ FA|EN مربعی کن.  
6. دکمه‌های اصلی را چهارخطی کن.  
7. تصاویر شاخص را clip متوازی‌الاضلاع بده.  
8. کارت‌ها بوردر `--line` بدون سایه نرم.  
9. ارقام را localize کن.  
10. کرسر لوزی را فقط دسکتاپ اضافه کن.  
11. ادمین را طبق بخش ۷ بساز یا پوسته‌اش را عوض کن.  
12. `vercel.json` / معادل SPA rewrite.  
13. با عرض ۳۷۵ و ۱۴۴۰ و RTL/LTR تست کن.

پس از این سیزده مورد، سایت از نظر تم عضو خانواده است حتی اگر محتوا فرق داشته باشد.

---

## ۱۳. توکن‌های آماده کپی (خلاصه اجرایی)

```css
html { background: #020810; color: #f2f5f9; }
a { color: inherit; text-decoration: none; }
button, input, select, textarea { border-radius: 0; font: inherit; }
:focus-visible { outline: 2px solid #1a75ff; outline-offset: 3px; }

.kicker { font-size: 12px; letter-spacing: 0.22em; text-transform: uppercase; color: #4d94ff; }
.section-title { font-size: clamp(28px, 4.4vw, 52px); font-weight: 500; line-height: 1.15; }
.lead { color: rgba(242,245,249,0.62); max-width: 62ch; }

.card { border: 1px solid #163c63; padding: 22px; background: rgba(15,40,66,0.35); }
.num { color: #0055FF; font-size: clamp(28px, 3vw, 44px); font-weight: 600; }
```

لوزی لوگو: `polygon(8,26 24,2 34,2 18,26)` در viewBox 42×28.  
ease: `cubic-bezier(0.22, 1, 0.36, 1)`.  
برند: `#0055FF` و هیچ رنگ اکشن دیگر.

---

## ۱۴. پرامپت آماده برای برنامه‌نویس / مدل هوش مصنوعی

متن زیر را همراه این فایل بده:

```
سایت/پنل را با سیستم طراحی ATW Dark Geometry بساز.

قفل‌ها:
- پس‌زمینه #020810، اکشن فقط #0055FF، هندسه تیز radius 0
- متوازی‌الاضلاع و لوزی زبان شکل است
- فارسی پیش‌فرض RTL، انگلیسی کامل LTR، ارقام مطابق زبان
- فونت FA: Modam → IRANSans → Vazirmatn | EN UI: Outfit | EN display: Syne
- دکمه اصلی چهارخطی است نه کپسول
- هدر sticky تیره، ناوبری با پر شدن آبی از پایین
- کارت بوردر #163c63 بدون گوشه گرد
- کرسر لوزی فقط pointer:fine
- حروف فارسی را به اسپن تک‌حرفی نشکن
- هاور فقط hover+fine
- پنل ادمین طبق بخش ۷ همین سند: سایدبار 240px، جدول خطی، KPI تیز، ورود بدون کارت سفید

ممنوع: rounded-full، طلا، بنفش، قفسه کتاب، Plus Jakarta به‌عنوان هویت، اسکرول افقی.

خروجی باید در 375px و دسکتاپ و FA/EN یک خانواده دیده شود.
```

---

**پایان سند.** اگر ظاهر با این فایل فرق داشت، ظاهر اشتباه است نه سند. رنگ برند را از UI تنظیمات قابل تغییر نگذار.
